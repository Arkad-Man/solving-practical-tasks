// Функция find(arr, value), которая ищет в массиве arr значение value
// и возвращает его номер, если найдено, или -1, если не найдено.

// let arr = ["test", 2, 1.5, false];

// const find = (arr, val) => {
//     for(let i = 0; i < arr.length; i++){
//         if(val === arr[i]){
//             return console.log(i);
//         }
//     }
//     return console.log(-1);
// }
// find(arr, "test");
// find(arr, 2);
// find(arr, false);
// find(arr, 5);
